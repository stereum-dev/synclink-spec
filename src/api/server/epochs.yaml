get:
  tags:
    - Synclink Server
  description: Get latest epochs
  operationId: getEpochs
  parameters: []
  summary: Get Epochs
  responses:
    "200":
      description: Success
      content:
        application/json:
          schema:
            type: object
            properties:
              status:
                type: string
                example: success
              epochs:
                type: array
                items:
                  $ref: ../../types/synclink.yaml#/Epoch
